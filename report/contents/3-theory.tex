\section{МЕТОДЫ ЦЕНТРАЛИЗОВАННОГО УПРАВЛЕНИЯ АВТОНОМНЫМИ АГЕНТАМИ}

В данной главе будут рассмотрены различные методы планирования путей, а также методы 
централизованного разрешения конфликтов, возникающих в ходе непосредственного следования агентами по 
спланированным маршрутам.

\subsection{Описание исходных данных}
% исходные данные для по тех заданию без подробностей
Исходные данные для задачи планирования представляют из себя две сущности --- окружение, в котором 
перемещаются агенты, и сами агенты. Так как в рамках данной задачи интерес представляет глобальное
поведение агентов, окружение можно представить в виде взвешенного графа из $n$ вершин и $m$ ребер:

\begin{equation}
    G = (V, E, W),
\label{e:graph}
\end{equation}

где $V$ --- множество вершин $\left\{ v_1, \ldots, v_n \right\}$,\par
$E$ --- множество ребер ($e = \left\{ v_i, v_j \right\}$),\par
$W : E \rightarrow \mathbb{R}$ --- функция, задающая вес ребра.

В таком графе ребрам могут соответствовать улицы, отдельные участки улиц, комнаты или просто некоторые 
ограниченные части пространства.

Другой частью исходных данных считаем множество из $k$ пронумерованных агентов:

\begin{equation}
    A = \left\{ 1, \ldots, k \right\}.
\label{e:agents}
\end{equation}

% исходные данные MAPF
В такой постановке исходные данные этой задачи очень похожи на исходные данные 
задачи многоагентного планирования MAPF --- Multi Agent Path Finding \cite{mapf-definition}.

Исходные данные классической задачи многоагентного планирования представляют из себя граф 
$G = (E, V)$ и множество агентов $A$~(\ref{e:agents}). Для каждого агента $i \in A$ определены 
начальная (стартовая) $s_i \in V$ и конечная (целевая) $g_i \in V$ вершины графа. Для классического 
MAPF характерно использование дискретного времени, что выражается в использовании невзвешенного графа
и допущениях о перемещении агентов, для которых любое действие (перемещение по ребру или ожидание) 
занимает фиксированное время.

Такая модель плохо подходит для описания реальных ситуаций, в которых разные действия могут иметь 
разную продолжительность, агенты перемещаются с переменной скоростью или дискретизация пространства 
приводит к чрезмерному увеличению размерности задачи.
Поэтому для многоагентного планирования существует также формулировка \cite{ccbs}, в которой 
используется взвешенный граф $G$~(\ref{e:graph}). Вес ребра отражает его длину и агенты перемещаются по 
ребру в течении некоторого времени, которое определяется их скоростью.

% итоговая исходные данные
Таким образом в качестве исходных данных в работе будет использоваться взвешенный граф $G$~(\ref{e:graph}) 
и множество агентов $A$~(\ref{e:agents}). Так как речь идет об определенных окружениях для агентов,
предполагается, что графы представляют из себя неориентированные планарные графы вершины которых 
расположены на двумерной плоскости $V \subset \mathbb{R}^2$, а ребра --- отрезки между 
соответсвующими точками. В качестве веса ребра используется его длина:

\begin{equation}
\begin{split}
    &W : E \rightarrow \mathbb{R}_{+} \\
    &W(e) = ||e||_2,
\end{split}
\end{equation}

где $e \in E$.

Помимо геометрических характеристик, каждому ребру сопоставляется признак, отражающий его
пропускную способность. Часть рёбер графа соответствует достаточно широким участкам среды, на
которых одновременное присутствие нескольких агентов и их встречное движение физически возможны
без столкновений. Остальные рёбра соответствуют узким участкам, на которых в каждый момент времени
допустимо движение агентов только в одном направлении. Это разделение задаётся вместе с исходным
графом в виде разбиения множества рёбер:

\begin{equation}
    E = E_{w} \sqcup E_{n},
\label{e:edges-split}
\end{equation}

где $E_{w}$ --- множество широких рёбер,\par
$E_{n}$ --- множество узких рёбер.

\subsection{Методы многоагентного планирования}

MAPF --- важный класс задач многоагентного планирования, в котором требуется построить маршруты для
нескольких агентов с ключевым ограничением: агенты должны иметь возможность следовать по этим маршрутам
одновременно, не сталкиваясь друг с другом. MAPF находит широкое применение в автоматизированных складах,
автономных транспортных средствах и робототехнике.

\subsubsection{Постановка задачи}

% классическая постановка
Постановка классической задачи MAPF выглядит следующим образом. Исходные данные описаны в предыдущем
разделе и представляют из себя граф и множество агентов. Предполагается, что время дискретно: на 
каждом шаге каждый агент находится в одной из вершин графа и
выполняет ровно одно действие. Действие в классической задаче MAPF --- это функция $a : V \to V$,
где $a(v) = v'$ означает, что агент, находящийся в вершине $v$ и выполняющий действие $a$, окажется
в вершине $v'$ на следующем шаге. Каждый агент может выполнять два типа действий: ожидание и
перемещение. Ожидание означает, что агент остаётся в текущей вершине ещё один шаг, перемещение ---
что агент переходит из текущей вершины $v$ в смежную вершину $v'$ графа.

Для последовательности действий $\pi = (a_1, \ldots, a_n)$ и агента $i$ обозначим через $\pi_i[x]$
положение агента после выполнения первых $x$ действий из $\pi$, начиная из стартовой вершины $s_i$:

\begin{equation}
    \pi_i[x] = a_x\!\left(a_{x-1}\!\left(\cdots a_1(s_i)\right)\right).
\end{equation}

Последовательность действий $\pi$ является индивидуальным планом агента $i$ тогда и только тогда,
когда выполнение этой последовательности из $s_i$ приводит агента в целевую вершину $g_i$, то есть
$\pi_i[|\pi|] = g_i$. Решением задачи называется набор из $k$ индивидуальных планов --- по одному
для каждого агента.

Основная цель алгоритмов решения MAPF --- найти решение, 
которое может быть выполнено без столкновений. Для этого в процессе планирования используется понятие
конфликта: решение называется допустимым тогда и только тогда, когда между любыми двумя
индивидуальными планами нет конфликтов. Определение конфликта зависит от модели окружения, обычно 
выделяют несколько видов конфликтов ($\pi_i$ и $\pi_j$ --- пара
индивидуальных планов) \cite{mapf-definition}:

\begin{itemize}
    \item \textbf{Конфликт в вершине} возникает между $\pi_i$ и $\pi_j$, 
    когда согласно этим планам агенты оказываются в одной вершине в один и тот же момент
    времени. То есть существует шаг $x$ такой, что $\pi_i[x] = \pi_j[x]$.

    \item \textbf{Конфликт в ребре} возникает между $\pi_i$ и $\pi_j$, 
    когда агенты проходят по одному и тому же ребру в одном направлении на одном и том же
    шаге. То есть существует шаг $x$ такой, что $\pi_i[x] = \pi_j[x]$ и $\pi_i[x+1] = \pi_j[x+1]$.

    \item \textbf{Конфликт следования} возникает между $\pi_i$ и $\pi_j$, 
    когда один агент занимает вершину, которую другой агент покинул на предыдущем шаге.
    То есть существует шаг $x$ такой, что $\pi_i[x+1] = \pi_j[x]$.

    \item \textbf{Циклический конфликт} возникает между набором планов
    $\pi_i, \pi_{i+1}, \ldots, \pi_j$, когда на одном и том же шаге
    каждый агент перемещается в вершину, которую только что покинул другой агент. 
    То есть существует шаг $x$ такой, что 
    $\pi_i[x+1] = \pi_{i+1}[x]$, \; $\pi_{i+1}[x+1] = \pi_{i+2}[x]$, \; $\ldots$, \;
    $\pi_{j-1}[x+1] = \pi_j[x]$, \; $\pi_j[x+1] = \pi_i[x]$.

    \item \textbf{Конфликт обмена} возникает между $\pi_i$ и $\pi_j$, когда агенты меняются 
    местами за один шаг. То есть существует шаг $x$ такой, что
    $\pi_i[x] = \pi_j[x+1]$ и $\pi_j[x] = \pi_i[x+1]$.
\end{itemize}

% уточнение для непрерывного времени
Использование дискретного времени ограничивает применимость методов решения классической задачи. 
Для возможности работы с непрерывным временем используется более сложная модель, в которой граф, 
описывающий возможные положения агентов, является взвешенным. В таком случае различные действия 
могут иметь различную продолжительность: время перемещение агента зависит от его скорости, время 
ожидания неограниченно и так далее.

В условиях непрерывного времени не имеет смысла разделение конфликтов на категории, так как конфликт 
может произойти в любой момент времени между агентами, находящимися в процессе выполнения своих действий. 
Вместо этого, например, может быть использовано определение конфликтов на основе пересечения 
геометрических форм агентов \cite{ccbs}.

% уточнение lifelong
Другой особенностью классической (даже непрерывной) задачи является то, что она представляет из себя 
только \enquote{одноразовый} вариант фактической проблемы во многих приложениях. Обычно после того,
как агент достигает своего целевого местоположения, он не останавливается и не ждет там вечно. 
Вместо этого ему назначается новое целевое местоположение, и от него требуется продолжать двигаться.
Такой вариант называется продолжительным (lifelong) MAPF \cite{rhcr} и характеризуется постоянным
назначением агентам новых целевых местоположений.

\include{contents/3-1-mapf.tex}

\subsection{Методы разрешения конфликтов}

Описанные выше методы многоагентного планирования строят индивидуальные маршруты агентов с учётом 
возможных конфликтов уже на этапе поиска, что в общем случае требует значительных вычислительных 
ресурсов и существенно усложняет реализацию, особенно в условиях непрерывного времени и 
продолжительного режима работы.

На практике зачастую есть возможность использовать механизмы способные непосредственно 
воздействовать на движение агентов в процессе исполнения. То есть конфликты могут быть исключены и без 
учёта взаимодействий на этапе планирования. Это позволяет использовать простые и быстрые 
одноагентные алгоритмы поиска пути, перенося задачу согласования движения на отдельный уровень 
управления. Получаемые таким образом совместные планы могут уступать в оптимальности решениям, 
найденным полноценными многоагентными алгоритмами, однако в продолжительном режиме работы, где 
маршруты постоянно перестраиваются, такой компромисс между качеством решения и стоимостью его 
получения часто оказывается оправданным. В этом разделе рассматриваются методы, реализующие подобный подход к 
разрешению конфликтов.

\subsubsection{Разрешение конфликтов путем управления разъедом агентов}

\subsubsection{Предотвращение конфликтов при помощи глобальных ограничений}

Другой способ исключить столкновения между агентами --- заранее ограничить множество допустимых 
совместных состояний так, чтобы конфликты были невозможны по построению. Такие ограничения задаются 
глобально на уровне самого графа $G$~(\ref{e:graph}) и не зависят от текущих маршрутов агентов; 
индивидуальные планы строятся независимо для каждого агента, а согласование движения обеспечивается 
выполнением общих правил прохождения отдельных участков среды.

Рассматриваемый ниже метод опирается на введённое ранее разбиение множества рёбер
$E = E_{w} \sqcup E_{n}$~(\ref{e:edges-split}) на широкие и узкие. На широких рёбрах конфликт между
агентами физически невозможен и никаких дополнительных ограничений не требуется; на узких рёбрах,
напротив, сосредоточены все потенциальные столкновения, и согласование движения агентов проводится
именно на этих участках.

С такой точки зрения, узкие ребра можно рассматривать как общие ресурсы с ограниченным доступом,
а задачу управление агентами --- как задачу синхронизации разрешений на доступ к этим ресурсам. 
Для решения этой задачи могут быть использованы различные примитивы синхронизации, например --- семафоры.

Итак, каждому узкому ребру $e = \{u, v\} \in E_{n}$ сопоставляется семафор --- глобальный объект,
регулирующий проход агентов через $e$. Состояние семафора в момент времени $t$ описывается не просто 
счетчиком обращений, а тройкой:

\begin{equation}
    \sigma_e(t) = \left( d_e(t),\, I_e(t),\, Q_e^{u}(t),\, Q_e^{v}(t) \right),
\end{equation}

где $d_e(t) \in \{u, v, \varnothing\}$ --- разрешённое направление движения по ребру 
(вершина, с которой агенты входят на $e$; $\varnothing$ означает, что ребро свободно),\par 
$I_e(t) \subseteq A$ --- множество агентов, находящихся в данный момент на ребре $e$, \par
$Q_e^{u}(t)$ --- очередь агентов, ожидающих прохода со стороны вершины $u$, \par
$Q_e^{e}(t)$ --- очередь агентов, ожидающих прохода со стороны вершины $e$.

На исполнение маршрутов агентами накладывается следующее ограничение: агенту $i \in A$ разрешено 
начать перемещение по ребру $e \in E_{n}$ из вершины $u$ в момент $t$ тогда и только тогда, 
когда $d_e(t) = u$ и $i \notin Q_e^{u}(t) \cup Q_e^{v}(t)$. В противном случае агент обязан ожидать 
в вершине $u$ и помещается в очередь $Q_e^{u}(t)$; ожидание продолжается до тех пор, пока семафор 
не разрешит проход в направлении $u \to v$.

Эволюция состояний семафоров во времени подчиняется двум правилам, гарантирующим отсутствие конфликтов 
на узких рёбрах. Во-первых, пока на ребре находится хотя бы один агент, направление прохода не может 
быть изменено на противоположное:

\begin{equation}
    I_e(t) \neq \varnothing \;\Rightarrow\; d_e(t') = d_e(t)
    \text{ при } I_e(t') \cap I_e(t) \neq \varnothing.
\end{equation}

Во-вторых, как только ребро освобождается ($I_e(t) = \varnothing$), семафор выбирает новое направление 
прохода исходя из состояния очередей $Q_e^{u}(t)$ и $Q_e^{v}(t)$; выбор производится по различным политикам, 
например обеспечивающей минимальную задержку (первой пропускается сторона, которая дольше ожидала очереди)
или минимизирующая число агентов в состоянии ожидания (первой пропускается сторона, на которой скопилось больше агентов).
Все агенты, ожидающие со стороны выбранной вершины, получают разрешение на проход и переводятся в множество 
$I_e$, после чего могут одновременно двигаться по ребру в общем направлении.

Упрощённый псевдокод работы системы семафоров в некоторый момент времени приведён на
рисунке~\ref{alg:semaphores}. На каждом шаге для каждого агента, находящегося в начале узкого
ребра, проверяется состояние соответствующего семафора, после чего пересматривается состояние всех
семафоров и при необходимости переключается направление прохода.

\begin{figure}
\begin{minipage}{\linewidth}
\begin{algorithm}[H]
    \SetAlgoVlined
    \KwIn{Множество агентов $A$, состояния семафоров $\{\sigma_e\}_{e \in E_{n}}$, момент времени $t$}
    \ForEach{агент $i \in A$, готовый войти на узкое ребро $e = \{u, v\} \in E_{n}$ из вершины $u$}{
        \If{$d_e(t) = u$}{
            добавить $i$ в $I_e(t)$ и разрешить перемещение\;
        }
        \Else{
            добавить $i$ в очередь $Q_e^{u}(t)$ и перевести в состояние ожидания\;
        }
    }
    \ForEach{агент $i \in A$, завершивший проход по ребру $e \in E_{n}$}{
        удалить $i$ из $I_e(t)$\;
    }
    \ForEach{узкое ребро $e \in E_{n}$ с $I_e(t) = \varnothing$}{
        выбрать новое направление $d_e(t)$ по политике обслуживания очередей $Q_e^{u}(t)$, $Q_e^{v}(t)$\;
        перевести всех агентов из очереди со стороны $d_e(t)$ в $I_e(t)$ и разрешить им движение\;
    }
\end{algorithm}
\end{minipage}
\caption{Псевдокод управления агентами при помощи системы семафоров}
\label{alg:semaphores}
\end{figure}

Из приведённых правил непосредственно следует, что в любой момент времени все агенты, находящиеся на
одном узком ребре $e$, движутся в одном направлении, а на широких рёбрах ограничения вообще не
накладываются. Таким образом, конфликты обмена и встречные конфликты на ребре исключаются по построению,
от агента требуется лишь обладать возможностью остановиться и ожидать, что уже является подзадачей локального управления.

\subsection{Метрики}

Для сравнения различных методов централизованного управления автономными агентами требуется ввести
количественные показатели качества получаемых решений.

% классические метрики
В классической постановке задачи MAPF решением является набор индивидуальных планов
$\Pi = \{\pi_1, \ldots, \pi_k\}$, обозначим момент, в который агент $i$
впервые оказывается в своей целевой вершине $g_i$, как $|\pi_i|$. 
В качестве показателей качества такого решения
в литературе \cite{mapf-definition} наиболее часто используются следующие метрики.

Общее время выполнения (makespan) определяется как момент, к которому все агенты достигают
своих целей, и характеризует общее время исполнения решения:

\begin{equation}
    \mathrm{MS}(\Pi) = \max_{1 \leq i \leq k} |\pi_i|.
\label{e:makespan}
\end{equation}

Сумма стоимостей (sum of costs, flowtime) определяется как суммарное время,
затраченное всеми агентами на достижение целей:

\begin{equation}
    \mathrm{SOC}(\Pi) = \sum_{i = 1}^{k} |\pi_i|.
\label{e:soc}
\end{equation}

В отличие от $\mathrm{MS}$, $\mathrm{SOC}$ поощряет решения, в которых агенты в среднем достигают
целей раньше, и наиболее распространена в работах по поисковым алгоритмам MAPF, в том числе в
описанных выше CBS и CCBS. Близка по смыслу метрика суммы перемещений (sum of fuel), в
которой учитываются только действия перемещения, но не действия ожидания; она отражает суммарные
энергетические затраты на выполнение плана. Наконец, в постановках с жёстким ограничением на
время выполнения плана в качестве показателя качества используется число агентов, успевших достичь
своей цели до заданного момента $T$, то есть $|\{i \in A : |\pi_i| \leq T\}|$.

Помимо качества самого плана, обычно фиксируются и характеристики работы алгоритма ---
время планирования, число раскрытых узлов в дереве поиска, доля задач, решённых в пределах
заданного бюджета времени (success rate). Эти показатели важны при сравнении
вычислительной эффективности методов, но не отражают качество поведения агентов в среде.

% неприменимость к нашей задаче
Все перечисленные метрики разработаны для \enquote{одноразовой} постановки MAPF, в
которой для каждого агента задана единственная целевая вершина, а после выполнения построенного
решения задача считается завершённой. В рассматриваемой постановке такое допущение
неприменимо: как уже отмечалось, после достижения очередной цели агенту назначается новое целевое
местоположение, и однозначно выделенного момента окончания плана у агента $i$ нет. Поэтому
величина $|\pi_i|$ в классическом смысле не определена, а значения $\mathrm{MS}$~(\ref{e:makespan})
и $\mathrm{SOC}$~(\ref{e:soc}) неограниченно растут с увеличением горизонта наблюдения и не
позволяют сопоставлять методы между собой.

Кроме того, при разрешении конфликтов путём перепланирования план
$\pi_i$ может многократно изменяться в процессе работы системы, а момент достижения очередной цели
зависит не только от исходных данных, но и от истории взаимодействия с другими агентами. В таких
условиях метрики, опирающиеся на конкретный итоговый набор маршрутов, теряют смысл стационарного
показателя: их значения определяются моментом, в который остановлено наблюдение, а не свойствами
самого метода. Естественная для задач разрешения конфликтов метрика --- число возникающих
столкновений --- также неприменима, поскольку практические все рассматриваемые методы по построению
не допускают конфликтов между агентами и эта величина тождественно равна нулю.

Поэтому в качестве показателей качества в рассматриваемой задаче целесообразно использовать
интегральные характеристики потока заданий на фиксированном промежутке времени: число
достигнутых системой целей, среднее время обслуживания одного задания и среднюю скорость
движения агента по маршруту. Такие показатели прямо обобщают $\mathrm{SOC}$ и число завершённых
задач на непрерывный режим работы; их конкретный выбор и способ измерения описаны в главе с
результатами.

